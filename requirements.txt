# ─────────────────────────────────────────────
#  RIFT — Money Laundering Detection API
#  Root-level file — used by Railway, Render,
#  Fly.io and other platforms that scan the repo root.
#  Tested on Python 3.11 / 3.12
# ─────────────────────────────────────────────

# Web framework
fastapi==0.115.0

# Async file I/O — required by FastAPI StaticFiles
aiofiles==24.1.0

# ASGI server (standard includes httptools + uvloop for production speed)
uvicorn[standard]==0.32.0

# File upload support (required for UploadFile in FastAPI)
python-multipart==0.0.19

# Data validation (Pydantic v2 ships with FastAPI 0.100+)
pydantic==2.9.2

# Graph analysis engine
networkx==3.4.2

# Data parsing / CSV handling
pandas==2.2.3

# Pandas requires these — pinned to avoid numpy 2.x breakage
numpy==1.26.4
python-dateutil==2.9.0
pytz==2024.2
